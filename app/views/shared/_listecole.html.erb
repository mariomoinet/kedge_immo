<script>
$( function() {
  var availableTags = [
    "KEDGE MARSEILLE",
    "KEDGE BORDEAUX",
  ];
  $( "#tags" ).autocomplete({
    source: availableTags
  });
  $("#tags").attr("placeholder", "Ton école");
  $("#tags").blur(function(){
    val = $("#tags").val();
    if (availableTags.includes(val) == false ){
      $("#tags").val("");
    }

  });
} );
</script>
